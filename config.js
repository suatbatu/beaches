// Hello Beaches — optional settings. Edit, commit, push; there is no build step.
window.HELLO_BEACHES_CONFIG = {
  // Flickr API key from https://www.flickr.com/services/apps/create/ (a public, non-secret key is fine here).
  // Leave empty to show geo-tagged photos from Wikimedia Commons instead.
  flickrApiKey: '',
};
